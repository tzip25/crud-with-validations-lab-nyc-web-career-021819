<% @songs.each do |song| %>
<table>
  <tbody>
    <tr>
      <td><%= link_to song.title, song %> - </td>
      <td><%= song.artist_name %></td>
    </tr>
  </tbody>
</table>
<% end %>
<h2><%= link_to "Add New Song", new_song_path %></h2>
